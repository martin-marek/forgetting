# Instruction-tuned Llama experiment

This experiment is the only one in the repository that uses the separate
`ptx` training codebase, copied here as `ptx/`. The other experiments use the
main code in the repository root.
